[package]
name = "tuxguitar"
version = "0.1.0"
edition = "2024"
description = "Parser and writer for TuxGuitar .tg files"
license = "MIT"

[dependencies]
roxmltree = "0.21"
quick-xml = "0.40"
zip = { version = "8", default-features = false, features = ["deflate"] }
